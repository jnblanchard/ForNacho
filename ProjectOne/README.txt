USAGE

$ make all
$ ./MCP

USAGE

The first line of main() - line 117
Edit the first arguement of populateArray to spawn more or less children.

DEFAULT - std::vector<InFunct> functions = populateArray(20);
N Children - std::vector<InFunct> functions = populateArray(n);
